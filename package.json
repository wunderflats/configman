{
  "name": "@wunderflats/configman",
  "version": "2.4.0",
  "description": "Tool for defining and checking environment variables in one place.",
  "author": "Wunderflats GmbH",
  "license": "MIT",
  "type": "commonjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/wunderflats/configman.git"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "start": "node lib/index.js",
    "test": "node --test test/",
    "format": "echo 'Use `npm run check` or `npm run check:write` instead' && exit 1",
    "lint": "echo 'Use `npm run check` or `npm run check:write` instead' && exit 1",
    "check:write": "biome check --write",
    "check": "biome check",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "husky": "9.0.11",
    "np": "10.2.0"
  },
  "dependencies": {
    "debug": "4.1.1"
  }
}
